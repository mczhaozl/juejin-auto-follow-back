---
description: 单文件代码行数上限，便于维护与 code review
alwaysApply: true
---

# 单文件行数上限

- 每个源文件（含 `redbook/**/*.js`、`scripts/**/*.js`、组件与工具模块）**代码行数不超过 300 行**（不含空行与注释可适当放宽，但总行数建议 ≤300）。
- 超出时拆分为多个模块（如 `redbook/utils/` 下按步骤拆分），主流程只做编排与调用。
