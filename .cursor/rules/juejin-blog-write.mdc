---
description: 掘金文章默认万字、少量多次写入、单次最多 40 行；生成或编辑掘金/ databae 文章时遵守
alwaysApply: true
---

# 掘金文章编写规范

- **默认篇幅**：生成的掘金技术文章**默认为万字长文**（约 1 万中文字），除非用户明确要求短文、简要或指定字数。
- **少量多次写入**：必须使用 **Write**、**StrReplace**、**fsAppend** 等工具通过多轮写入完成正文，**不得**一次性写入整篇长文。
- **单次最多 40 行**：每一轮 `Write` / `StrReplace` / `fsAppend` 写入到同一文件的内容**不得超过 40 行**（按写入块的行数计）；超出时拆成多轮写入。
- 适用场景：使用 juejin-blog 技能写文章、向 `databae/**` 或 `posts/**` 等落盘文章时，均须遵守上述篇幅与写入方式。

## databae 文章格式规范

### index.md 格式要求

**必须包含 `---` 元数据分隔符**，结构如下：

```markdown
# 文章标题

> 一句话摘要（50-100 字）

---

## 一、正文第一节
正文内容...
```

- 第一个 `---` 位于「摘要」之后、「正文第一节」之前
- 脚本通过 `---` 分隔元数据（标题+摘要）和正文
- 如果没有 `---` 或 `---` 位置不正确，会导致解析失败、正文为空

### config.json 格式要求

```json
{
  "title": "文章主标题",
  "brief": "一句话摘要，50-100 字符，超出会导致发布接口报错",
  "categoryId": "分类 id",
  "tagIds": "标签 id1,标签 id2",
  "publish": true,
  "themeIds": "话题 id（可选）",
  "columnIds": "专栏 id（可选）",
  "cover_image": "封面图 URL（可选）"
}
```

- **brief 长度必须在 50-100 字符之间**，否则发布接口会报错
- **必须包含 categoryId 和 tagIds**，否则会跳过上传
- publish 默认为 true（立即发布），设为 false 可存草稿
