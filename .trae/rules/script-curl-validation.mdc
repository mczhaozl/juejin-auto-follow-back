---
description: 每个脚本需通过本地 curl 验证才算合格
alwaysApply: true
---

# 脚本验收：本地 curl 通过才算合格

凡**依赖外部 API 的脚本**（签到、回关、收藏、评论、沸点等），在视为完成前必须满足：

- **能用对应的本地 curl 请求复现成功**：该脚本所调用的接口，至少有一条可在本机执行并通过的 curl 命令（或等价请求）作为验证依据。
- 未通过本地 curl 验证的脚本，不算合格，需先修到 curl 能成功再合入或交付。

## 原则

- **先 curl 后脚本**：新增或修改调用掘金（或其它）API 的脚本时，先确认接口能用 curl（含 Cookie/CSRF/msToken/a_bogus 等必要参数）调通，再在脚本里复用同一套 URL、方法、头、体。
- **curl 即契约**：把「能跑通的 curl」当作该接口的契约；脚本里的请求构造（URL、query、body、headers）应与之一致，避免缺参、错误编码或多余字段导致风控/空响应。
- **文档可追溯**：建议在项目内（如 `test/*.txt`、脚本注释或 README）保留一份**可成功执行的 curl 示例**，便于日后排查与回归。

## 实践建议

### 新增脚本时

1. 用浏览器或抓包拿到目标接口的**完整请求**（URL、Method、Headers、Body）。
2. 在本机用 curl 复现该请求，确认返回预期（如 `err_no: 0` 或 200 + 正常 JSON）。
3. 再在脚本中实现相同逻辑（含 Cookie 清洗、可选 CSRF/msToken/a_bogus 等）；若脚本仍失败，对比脚本请求与「已通过的 curl」的差异（编码、缺参、过期 token 等）。

### 修改已有脚本时

- 若改了 URL、参数或请求体，需再次用**本地 curl** 验证修改后的请求能成功，再视为合格。

### 不依赖外部 API 的脚本

- 纯本地逻辑、无网络请求的脚本，可不要求 curl，但若有对外调用的子逻辑，仍建议对子逻辑对应的接口做 curl 验证。

## 合格标准小结

- **有对外 API 调用的脚本**：必须存在至少一条在本机可执行且成功的 curl（或等价请求），且脚本的请求构造与该 curl 一致。
- **无对外 API 的脚本**：不强制 curl，按项目其它规范验收即可。
